# 辽宁国省干线真实样例适配设计

日期：2026-07-07

## 1. 背景

`2026-07-07-liaoning-trunk-word-rules-design.md` 的规则模块化已实施完成，动态夹具测试全部通过。但对用户提供的真实非正式报告（绕阳河二号桥样例）执行 `/imports/word/parse` 时返回 `rating_table_not_found`。

读取层诊断（Task 3 的 `docx_diagnostics` 与 customXml 下钻）确认五张目标表都能读取，失败原因在规则层与真实样例的三处结构差异：

1. 病害表列名与夹具不同。真实表头为：

   ```text
   序号 | 部件名称 | 构件编号 | 病害位置 | 病害类型 | 病害特征 | 标度 | 病害扣分 | 构件评分 | 照片编号
   ```

   当前列查找按 `构件/位置/病害/数量/尺寸` 关键词匹配，且“病害”子串会先命中“病害位置”列。

2. `表4.1-2 总体技术状况评定表` 是矩阵布局，不是“层级”行布局。真实表每行是一个评价部件的分数行，全桥总分、综合评级、结构评分、权重、等级在合并单元格中逐行重复：

   ```text
   结构 | 类别 | 评价部件 | 构件数量 | 构件评分 | 桥梁部件技术状况评分 | 桥梁结构技术状况评分 | 桥梁结构组成权重 | 等级 | 桥梁总体技术状况评分 | 综合评级
   上部结构 | 1 | 上部承重构件 | 1个 | 55.81 | 86.62 | 87.45 | 0.4 | 2 | 85.61 | 2类
   ```

   当前解析器按“层级”列区分全桥/结构分部/评价部件行，对矩阵布局完全失效。

3. 病害照片题注在独立的小表格单元格里，文字位于图片下方，不在普通段落中。题注文本从 Word XML 读取时可能表现为 `照片2.11`、`照片2.21` 这类紧凑格式，需要规范化为 `2.1-1`、`2.2-1`。

其他诊断结论：

1. 表号中的连字符是 `w:noBreakHyphen` 元素，python-docx 文本读取正确，原始 XML 字符串搜索会漏。
2. 病害表存在跨页续表（表题相同、表头为空），现有表头校验已能拒绝，无需处理。
3. `表4.1-1 桥梁部件权重计算表` 表题识别正常。

## 2. 目标

1. `辽宁国省干线` 规则能解析真实非正式报告的三张病害表，产出病害候选。
2. 能解析矩阵布局的 `表4.1-2`，产出 `overall`、`structure_parts`、`evaluation_parts`。
3. 病害照片题注扫描扩展到表格单元格。
4. 保持动态夹具（“层级”布局、旧列名）继续通过，两套布局/表头都受测试覆盖。
5. 真实样例端到端调用 `/imports/word/parse` 返回 200 与合法契约 JSON。

本设计不负责：

1. 自动模板识别。
2. `附录1 桥梁技术状况评定表` 或正文评分兜底。
3. `.doc`、PDF、OCR。
4. 照片编号域自动刷新。导入前用户手动全选并按 F9 刷新仍是流程前提；如果 XML 文本中能读到紧凑编号，导入器会做规范化匹配。
5. 病害扣分、构件评分列入契约。评分以 `表4.1-2` 为准。

## 3. 当前实现决策

| 决策 | 当前实现 |
| --- | --- |
| 完好行处理 | 不专门跳过。`基本完好` 行可生成普通候选，`measurements=[]`，不再写尺寸低置信 warning，由人工校对阶段决定保留或忽略 |
| 构件字段映射 | `component_name` = 部件名称或构件名称类列，`component_alias` = 构件编号类列 |
| 标度、病害扣分、构件评分 | 第一版不进入 `BridgeAnnualInspectionData` 契约；评分仍以 `表4.1-2` 为准 |
| 模块 03 契约 | 本轮不变更契约，`DefectCandidate.severity` 继续按现有实现输出 `null` |

## 4. 病害表列别名

### 4.1 别名定义

`WordRuleSet` 新增病害表列别名，列查找改为按别名优先级匹配：对每个字段依次尝试各别名，先规范化全等，再子串包含；命中即停。

| 契约字段 | 别名优先级 | 真实表命中 | 旧夹具命中 |
| --- | --- | --- | --- |
| component_name | 部件名称、构件名称、构件、部件（排除构件编号/构件评分） | 部件名称 | 构件 |
| component_alias | 构件编号 | 构件编号 | 无（None） |
| defect_location | 病害位置、位置、部位 | 病害位置 | 位置 |
| defect_type | 病害类型、病害 | 病害类型 | 病害 |
| measurement_text | 病害特征、尺寸 | 病害特征 | 尺寸 |
| quantity_text | 数量 | 无（None） | 数量 |
| photo_numbers | 照片编号、照片 | 照片编号 | 照片编号 |

表头文本匹配前做规范化（去空白，兼容“部件 名称”这类带空格表头）。

### 4.2 当前保守处理

空的 `defect_type`、`defect_location` 沿用现有回退（`未识别病害`、`未识别位置`）。当前实现不把 `/` 和 `基本完好` 行自动删除，避免在导入原型阶段误丢数据；这些行进入普通候选，后续由人工校对选择保留或忽略。

`measurement_text` 为 `基本完好` 时，不生成结构化尺寸，也不写 `measurement_parse_low_confidence`。

### 4.3 真实样例预期

- 表2.1-1：12 条候选。
- 表2.2-1：7 条候选。
- 表2.3-1：6 条候选。
- 共 25 条候选，`photo_numbers` 从“照片2.1-1 照片2.1-2”样式解析出编号本体。

## 5. 表4.1-2 矩阵布局

### 5.1 布局识别

`表4.1-2` 支持两种布局，均在规则集中定义表头要求：

1. `层级` 布局（现状，动态夹具使用）：表头含 层级/结构部位/类别编号/评价部件/评分/权重/等级/构件评分。
2. `矩阵` 布局（真实样例）：表头含 结构/类别/评价部件/构件数量/构件评分/桥梁部件技术状况评分/桥梁结构技术状况评分/桥梁结构组成权重/等级/桥梁总体技术状况评分/综合评级。

表题命中 `表4.1-2` 后，先按矩阵表头识别，再按层级表头识别，两者都不满足则继续找下一张表。

### 5.2 矩阵解析

1. 逐数据行读取，`结构` 列不属于 上部结构/下部结构/桥面系 的行跳过。
2. `evaluation_parts[]`：按（结构、类别、评价部件）聚合。`part_score` 取 桥梁部件技术状况评分；每行的（构件数量、构件评分）追加为 `score_rows[]`，“1个”解析为 1。
3. `structure_parts[]`：按 结构 列首次出现去重。`structure_score` 取 桥梁结构技术状况评分，`weight` 取 桥梁结构组成权重，`grade` 取 等级。
4. `overall`：取首个非空的 桥梁总体技术状况评分 与 综合评级。
5. 不重新计算评分，只抽取 Word 已有值。

### 5.3 真实样例预期

- `overall.total_score = 85.61`，`overall_grade = 2类`。
- `structure_parts`：上部结构 87.45/0.4/2，下部结构 86.61/0.4/2，桥面系 79.93/0.2/3。
- `evaluation_parts`：上部承重构件（3 个 score_rows）、上部一般构件、支座、翼墙耳墙等。

## 6. 照片题注扩展

### 6.1 图片所在结构优先

当前实现不新增 `DocxBlocks.caption_texts`。`photo_extractor` 直接读取 `word/document.xml`：

1. 按 `a:blip` 顺序定位每一张图片。
2. 优先读取图片所在的单图单元格文本。
3. 其次读取只包含一张图片的内层表格文本。
4. 如果上述路径没有题注，再回退到普通段落题注扫描。

这样可以覆盖真实样例中“图片在表格里、说明文字在图片下方”的结构，并避免把病害检查表“照片编号”列误识别为图片题注。

### 6.2 紧凑编号规范化

真实样例中，部分题注从 XML 读取后会表现为：

```text
照片2.11  1-1#板横向裂缝
照片2.21  0#台左侧翼墙勾缝砂浆脱落
照片2.110  2-1#铰缝勾缝砂浆脱落
```

规则会规范化为：

```text
2.1-1
2.2-1
2.1-10
```

如果某份 Word 的图片题注确实读不到任何编号，图片仍会抽取到临时目录，病害行的 `photo_numbers` 也会保留；对应病害对象写 `photo_number_unmatched` warning，等待人工校对。

## 7. 错误与警告

本轮不新增错误码。其余错误码沿用现有定义：

| code | 位置 | 含义 |
| --- | --- | --- |
| `measurement_parse_low_confidence` | 病害对象 warning | 存在明显尺寸线索但无法稳定结构化 |
| `photo_number_unmatched` | 病害对象 warning | 病害表照片编号未匹配到图片题注 |
| `photo_not_referenced_by_defect` | 照片对象 warning | 图片题注未被病害表引用 |

## 8. 测试与验收

单元测试：

1. 真实表头（部件名称/构件编号/病害位置/病害类型/病害特征/标度/照片编号）能解析病害行，字段映射正确。
2. 旧夹具表头继续通过。
3. “病害”别名不误命中“病害位置”列。
4. `基本完好` 不触发尺寸低置信 warning。
5. 面积乘积、中文长度、中文总面积等真实尺寸表达能结构化。
6. 矩阵布局 `表4.1-2` 解析出 overall/structure_parts/evaluation_parts，评价部件 score_rows 聚合正确。
7. 层级布局继续通过。
8. 图片下方表格单元格中的题注能被识别；病害表照片编号列不被当作题注。
9. `照片2.11`、`照片2.110` 等紧凑编号能规范化为 `2.1-1`、`2.1-10`。

真实样例验收（本地执行，不提交样例）：

1. `/imports/word/parse` 返回 200。
2. 病害候选 25 条。
3. 病害照片候选 31 条，全部关联病害；临时图片文件 36 个。
4. 评分与 5.3 预期一致。
5. `measurement_parse_low_confidence` 为 0；`基本完好` 行 `measurements=[]` 且无尺寸警告。
6. 输出通过 `BridgeAnnualInspectionData` 校验。
7. 全部 Python 测试通过。

## 9. 取舍

1. 列别名与布局都放在规则集中，主体解析流程不感知“辽宁”字样，后续规则（吉林国省干线等）按同一接口扩展。
2. 不做 Word 域自动刷新——与模块 02“导入前手动 F9”前提一致；若 XML 中能读到紧凑编号，则规范化后匹配。
3. 病害扣分、构件评分两列本轮不入契约，避免与 `表4.1-2` 评分主来源产生第二事实源。
4. 完好行当前不自动跳过，避免原型阶段误丢表格行；是否忽略交给人工校对或后续规则优化。

## 10. 变更记录

| 日期 | 变更 | 原因 | 影响模块 |
| --- | --- | --- | --- |
| 2026-07-07 | 创建真实样例适配设计 | 真实报告与夹具在列名、表布局、题注位置三处不一致 | Word 导入规则模块 |
| 2026-07-07 | 修正为当前实现状态 | 真实样例已验证为 25 条病害候选、31 条病害照片候选、矩阵评分解析成功、尺寸低置信误报清零 | Word 导入规则模块 |
